[package]
name = "control-centre"
version.workspace = true
edition = "2021"

[[bin]]
name = "control-centre"
path = "src/main.rs"

[dependencies]
# Wayle's media state over the session bus.
zbus = "=5.18.0"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
