/* ROFI: base configuration */

configuration {
    /* ── Top-right corner ── */
    location: 3;
    x-offset: 5px;
    y-offset: 44px;
    scroll-method: 0;
    hover-select: true;
    cycle: false;
    
    /* ── Wayland ── */
    click-to-exit: true;

    modes: "combi,filesearch:rofi-filesearch,filebrowser";
    combi-modes: "drun,filesearch";
    combi-display-format: "{text}";
    sidebar-mode: true;

    /* ── Behaviour ── */
    /* Two-line rows (filesearch shows name + dir). Row height is measured once
       at startup, so this must be set globally rather than per-mode. */
    show-icons: true;
    icon-theme: "Papirus-Dark";
    display-combi: "󰍉 All ";
    display-filebrowser: " Folders ";
    display-filesearch: "󰈞 Files ";
    drun-display-format: "{icon} {name}";
    filesearch-display-format: "{icon} {name}";
    filebrowser-display-format: "  {name}";

    filebrowser {
        command: "xdg-open";
        directory: "/home/raina";
        show-hidden: false;
    }
}

@theme "~/.config/rofi/rofi-double.rasi"
